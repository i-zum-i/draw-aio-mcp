# Docker Environment Configuration for MCP Draw.io Server
# Copy this file to .env and configure your settings

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Anthropic API Key (REQUIRED)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server metadata
SERVER_NAME=MCP Draw.io Server
SERVER_VERSION=1.0.0
PROTOCOL_VERSION=2024-11-05

# Build configuration
VERSION=latest
BUILDTIME=

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Temporary files directory (inside container)
TEMP_DIR=/app/temp

# Draw.io CLI path (should be 'drawio' if installed globally)
DRAWIO_CLI_PATH=drawio

# Cache settings
CACHE_TTL=3600
MAX_CACHE_SIZE=100

# File management
FILE_EXPIRY_HOURS=24
CLEANUP_INTERVAL_MINUTES=60

# Request timeout (seconds)
REQUEST_TIMEOUT=300

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# =============================================================================
# DOCKER COMPOSE OVERRIDES
# =============================================================================

# Uncomment and modify these settings to override Docker Compose defaults

# Resource limits
# MEMORY_LIMIT=512M
# CPU_LIMIT=1.0
# MEMORY_RESERVATION=256M
# CPU_RESERVATION=0.5

# Health check intervals
# HEALTH_CHECK_INTERVAL=30s
# HEALTH_CHECK_TIMEOUT=10s
# HEALTH_CHECK_START_PERIOD=30s
# HEALTH_CHECK_RETRIES=3

# =============================================================================
# DEVELOPMENT SETTINGS
# =============================================================================

# Uncomment these for development environment
# LOG_LEVEL=DEBUG
# CACHE_TTL=300
# FILE_EXPIRY_HOURS=1

# =============================================================================
# PRODUCTION SETTINGS
# =============================================================================

# Uncomment these for production environment
# LOG_LEVEL=WARNING
# CACHE_TTL=7200
# FILE_EXPIRY_HOURS=48
# CLEANUP_INTERVAL_MINUTES=30

# =============================================================================
# SECURITY NOTES
# =============================================================================

# 1. Never commit this file with real API keys to version control
# 2. Use Docker secrets or external secret management in production
# 3. Regularly rotate your Anthropic API key
# 4. Monitor API usage and set appropriate rate limits
# 5. Use read-only containers in production when possible

# =============================================================================
# USAGE EXAMPLES
# =============================================================================

# Build and run with Docker Compose:
#   docker-compose up --build
#
# Run in development mode:
#   docker-compose -f docker-compose.yml up mcp-server-dev
#
# Run with custom environment file:
#   docker-compose --env-file .env.production up
#
# Scale the service:
#   docker-compose up --scale mcp-server=3